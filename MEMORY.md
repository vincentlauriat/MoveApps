---
name: move-apps-project
last_updated: 2026-07-14 (On-demand "Debug" window added: live per-step transfer trace via new DebugLogStore, opened from the main window's toolbar — not yet manually click-through-verified)
---

# Project Memory — MoveApps

## Purpose
`move-app.sh` migrates Vincent's ~126 development projects (~38 GB) from `~/Documents/Github` (iCloud Drive-synced) to `~/DevApps` (local, not iCloud-synced), one or a few at a time, keeping each project immediately usable (git intact, Python venvs working, no dangling absolute paths). Its one-shot bulk rollout is **complete** (see "FINAL ROLLOUT SUMMARY" below) — `move-app.sh` is now kept as an unmaintained CLI fallback/reference.

Since 2026-07-02, the active focus of this project is **MoveApps.app**, a native SwiftUI macOS app that replaces the CLI for ongoing, bidirectional GitHub⇄DevApps transfers across Vincent's Macs. See "MoveApps.app (SwiftUI rewrite) — project state" below and `PLAN.md` for the current phase.

## Current state
- **Full bulk rollout COMPLETE (2026-07-01 → 2026-07-02).** All 126 projects have been migrated from `~/Documents/Github` to `~/DevApps`. Tool built, tested read-only first (`--list`, `--dry-run` on `AuditViewer` and `clarify`), validated with one real migration (`LinkManager`), then rolled out in 7 batches of ~15, each migrated and independently re-verified (git branch/HEAD/dirty-count re-checked with `git status --porcelain`, venv sanity via real `pip list`, broken-symlink scan excluding build artifacts, grep for residual absolute paths, confirmation the source folder is gone).
- `~/Documents/Github` now contains only 3 non-project leftovers (`CLAUDE.md`, `CommitTousLesDossiers.sh`, `MetsAJourLesGIT.sh`) — 0 project folders remain.
- Batch 1/7 done (`Chronos`, `clarify`, `claude-hooks-demo`, `claude-mem`, `Claude.md.PlusPlus`, `ClaudeCodePontUX`, `ClaudeCommander`, `ClaudeControlPlane`, `ClaudeHooks`, `ClaudeSettings`, `CLI-Airtable`, `CLI-Plaud`, `CLI-Starter`, `CLIPrint`, `ContextManager`): 14/15 clean, 1 warning on `clarify` (see below). `claude-mem` (937M) and `ClaudeCommander` (698M) hit the known `mv`-timeout and fell back to `ditto` successfully.
- Batch 2/7 done (`curl.md`, `CV`, `datagouv-mcp`, `DiagnosticsReseau`, `Electron`, `feynman`, `Ficelles`, `Finance`, `FinceptTerminal`, `Gatsby`, `glances`, `GlobalTimeSheet`, `gmailreader`, `graphify`, `headroom`): 13/15 clean, 2 warnings (`Gatsby`, `glances`, see below). `FinceptTerminal` (6.0G, largest project moved so far), `feynman` (787M), `Finance`, `Gatsby` (1.1G), `glances` (784M), `headroom`, `curl.md` all hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 3/7 done (`hermes`, `homebrew`, `iCloudMonitor`, `immocarto`, `Inspirations`, `InternetCheck`, `InvestFinder`, `IPP-app`, `JobTracker`, `KnowledgeBuilder`, `KnowledgeLib`, `KnowledgeMarker`, `linkedin`, `llmfit`, `localhostmgr`): 15/15 clean, no warnings. `KnowledgeBuilder` (2.0G) has 10814 dirty git files but that count matches pre-migration exactly (already dirty before, not caused by the move). `immocarto`, `Inspirations`, `InvestFinder`, `IPP-app`, `KnowledgeMarker` hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 4/7 done (`LocalWebManager`, `LocationManager`, `LocaWeb`, `MarkdownViewer`, `Medium`, `MenuBarManager`, `minutes`, `MLXTest`, `my-video`, `MyTasks`, `NatureSite`, `nDocMake`, `NetCheck`, `NetDiscoPocket`, `NetDiscoWeb`): 13/15 clean, 2 warnings (`MyTasks`, `NetCheck`, see below). `MLXTest` (2.1G) venv recreated successfully with real packages (`mlx`, `mlx-lm`, etc). `LocalWebManager` (2.5G), `LocaWeb`, `Medium`, `MLXTest`, `my-video`, `MyTasks`, `NetCheck` hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 5/7 done (`NewsManager`, `Nexus`, `notion-cookbook`, `NotionBook`, `NotionCLI`, `NotionToObsidian`, `oh-my-claudecode`, `onyx`, `opendataloader-pdf`, `OpenTelemetry`, `Opentelemetry 2`, `OsxPhotos`, `Plaud`, `plaud-sdk-public`, `printing-press-library`): 14/15 clean, 1 warning on `onyx` — **real data loss, recovered**, see critical finding below. `oh-my-claudecode` (3302 dirty files, matches pre-migration exactly, not caused by the move). `NewsManager` (1.1G), `Nexus` (590M), `NotionBook`, `onyx` (1.0G), `printing-press-library` (883M) hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 6/7 done (`prompts.chat`, `ProjectTemplate`, `ProxyStory`, `RaybanMeta`, `RemoteClaude`, `rtk`, `RTKInfos`, `SkillManager`, `Skills`, `SkillsMarketInvestFinder`, `SkillsMgr`, `smartcolibri.fr`, `StockList`, `symphony`, `TeamsChat`): 14/15 clean, 1 warning on `symphony` (benign, see below). `StockList`'s (1.0G) venv recreated with real packages. `ProxyStory`, `RTKInfos` (714M), `SkillManager` (778M), `StockList` (1.0G), `symphony` (119M) hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 7/7 done, the FINAL batch (`TechDoc`, `TestNotionDoc`, `TestSites`, `toolkit`, `TVNews`, `UltraData`, `viewer-fixtures`, `VLTKYC`, `vMemory`, `Voltalia`, `VoltaliaTasks`, `WebAppUsage`, `WebManager`, `WhatsUp`, `WhatsUp.vibe.vincent`, `WifiManager`, `worldmonitor`, `zeroclaw` — 18 projects): 17/18 clean, 1 warning on `WifiManager` (same `.sparkle-tools` cross-project symlink issue as `NetCheck` in batch 4) — fixed the same way, re-pointed to `~/DevApps/MarkdownViewer/.sparkle-tools`. `TestSites`, `VoltaliaTasks` (675M), `WifiManager` (452M), `worldmonitor` (288M) hit the `mv`-timeout and fell back to `ditto` successfully.
- **121 total projects migrated** (13 pre-existing/manual + 108 via the 7 batches = 15×6 + 18). Rollout complete.
- Orphan empty venv `~/DevApps/LinkManager/path/to/venv` has been removed.
- Inventory at last scan: 76 git repos (remotes verified real for several, all pointing to GitHub), 30 Node projects, 30 Xcode projects, 8 Rust, 5 Python (pyproject.toml), 4 Python (requirements.txt), 1 Go. 8 real Python venvs found (`clarify`, `glances`, `LinkManager` — plus an orphan `path/to/venv` inside it —, `Gatsby/GatsbyExecution`, `Gatsby/GatbyViewer`, `Gatsby/MomentumBot`, `StockList/backend`, `MLXTest`).
- No `.icloud` (un-materialized) stub files found at last scan — all content was locally cached, so that code path is implemented but not yet exercised against a real stub.

## FINAL ROLLOUT SUMMARY — all 7 batches, complete picture
121 projects migrated total, 0 left in `~/Documents/Github`. Across all 108 batch-migrated projects: 100 fully clean, 8 flagged with a warning by the script. Of those 8:
- 3 were **empty Python venvs** (script's `pip freeze` 30s timeout too short): `clarify/.venv`, `Gatsby/GatbyViewer/venv`, `glances/.venv`. **All 3 fixed post-rollout** (2026-07-02): `glances` and `GatbyViewer` reinstalled from their `requirements.txt`; `clarify`'s venv only serves the `bmad-story-automator` skill tool (no runtime deps) so `story-automator` itself was installed editable — all verified working.
- 2 were **benign concurrent-edit false alarms** (dirty-count changed mid-move but only `M`/`??` entries, nothing lost): `MyTasks`, `symphony`.
- 2 were **broken cross-project symlinks**, both `.sparkle-tools` pointing to `MarkdownViewer`'s old path: `NetCheck`, `WifiManager` — both fixed by re-pointing to the new `~/DevApps/MarkdownViewer/.sparkle-tools`.
- **1 was real data loss, recovered: `onyx`** — `ditto` silently dropped 3 tracked files (183 lines of real code) during its fallback copy on a 1.0G repo. Recovered fully via `git restore` after confirming `.git fsck` was clean. This is the single most important finding of the whole rollout — see CRITICAL finding section below for full detail. It proves `ditto`'s fallback (used whenever plain `mv` hits the iCloud timeout, which was most large repos) is not 100% reliable on large trees, and the only reason it was caught was that all migrated projects started with a *known* pre-migration dirty count that the script diffs against.
- Several other projects had a lot of pre-existing dirty files (`KnowledgeBuilder` 10814, `oh-my-claudecode` 3302, `DiagnosticsReseau` 420, `WhatsUp.vibe.vincent` 70, `WhatsUp` 52, `rtk` 58) — all independently confirmed identical before/after, not caused by the migration.
- Python venvs that recreated successfully with real packages: `LinkManager/.venv`, `MLXTest/.venv` (mlx/mlx-lm), `Gatsby/MomentumBot/.venv` + `bot/venv` (trading bot, ~60 packages), `Gatsby/GatsbyExecution/venv`, `StockList/backend/.venv` (fastapi/anthropic/etc).

## Known issue found during testing
Plain `mv` between `~/Documents/Github/*` and `~/DevApps/*` can fail with `Operation timed out` even though both paths report the same filesystem device ID (`stat -f "%d"`) — iCloud Drive's FileProvider extension intercepts the `rename()` syscall. The script now falls back to `ditto` + verification automatically; this is not a hypothetical edge case, it reproduced on the very first real test (`LinkManager`), so expect it to trigger often on the remaining 125 projects.

## Known warning — clarify
`clarify`'s `.venv` was recreated empty (no `pip freeze` captured before the move). No `requirements.txt`/`pyproject.toml` at the project root either — it's a Node/Turbo monorepo, and the venv likely only serves the `bmad-story-automator` skill tool (`pyproject.toml` under `.claude/skills/bmad-story-automator/` and `.agents/skills/bmad-story-automator/`). Not blocking; flag to Vincent if he needs that skill's Python env — reinstall from that subpackage's `pyproject.toml`.

## Known warning — Gatsby and glances (batch 2)
`Gatsby` has 4 nested Python venvs; 3 recreated fine with real packages (`MomentumBot/.venv`, `MomentumBot/bot/venv` — a real trading-bot venv with ~60 packages incl. `ib-insync`/`anthropic`/`pandas`, and `GatsbyExecution/venv`), but `GatbyViewer/venv` came back empty. `glances`'s `.venv` also came back empty. Independently verified with `pip list` on each venv — confirms the script's own report. Root cause: the script's `pip freeze` capture has a 30s timeout (`with_timeout 30`), which can be too short for large/slow venvs before the move. Both `GatbyViewer` and `glances` do have `requirements.txt` (`glances` also has `pyproject.toml`) at their root, so a plain `pip install -r requirements.txt` after the move fixes it — just not automatic. Worth watching for on other large Python projects in later batches (e.g. `StockList/backend`, `MLXTest` still pending).

## Known warning — MyTasks and NetCheck (batch 4)
`MyTasks` finished with a NEW kind of warning, different from the empty-venv pattern: "nombre de fichiers modifiés différent avant/après (0 -> 6)" — the git dirty-count itself changed during the move window. Investigated with `git status --porcelain` / `git diff`: this is real, legitimate in-progress work (a design doc edit describing a `web/` subfolder restructuring, `.gitignore` change, new untracked `package.json`/`package-lock.json`/`.mcp.json`/`commandes.md`). Conclusion: something (another session/tool) was actively writing to `~/Documents/Github/MyTasks`'s working tree during the migration window, between the script's before/after git snapshots — not a migration bug, nothing lost or corrupted, just uncommitted local work that changed mid-move. Worth telling Vincent in case he had an editor/agent open on `MyTasks` he forgot about.

`NetCheck` finished with a warning for 4 broken symlinks. 3 are Xcode build/DerivedData artifacts under `build/` (self-referential, harmless — they regenerate on next build). The 4th was real: `.sparkle-tools` symlinked to `MarkdownViewer`'s `.sparkle-tools` folder at the old `Documents/GitHub/MarkdownViewer` path (cross-project symlink between two projects both migrated in this batch). Fixed directly: re-pointed `~/DevApps/NetCheck/.sparkle-tools` to `~/DevApps/MarkdownViewer/.sparkle-tools`, verified the link resolves and lists real Sparkle framework files. General lesson: cross-project symlinks (one project linking into another's folder) are a blind spot the script's own residual-path grep doesn't catch since it only scans file contents, not symlink targets — worth an eye out for more of these in later batches.

## CRITICAL finding — onyx (batch 5): ditto can silently drop tracked files
`onyx` finished with the same warning shape as `MyTasks` ("nombre de fichiers modifiés différent avant/après (0 -> 3)"), but the cause was different and more serious: `git status --porcelain` showed 3 files as **deleted** (`D`), not modified/added — `backend/onyx/connectors/dropbox/__init__.py`, `backend/onyx/connectors/dropbox/connector.py` (183 lines of real code), and `extensions/chrome/src/.DS_Store`. Confirmed on disk: the whole `dropbox/` connector folder was genuinely missing after the move, and the source at `~/Documents/Github/onyx` was already gone (no fallback copy). This means `ditto` silently failed to copy some files on this 1.0G repo during the `mv`-timeout fallback — a real bug/limitation, not a hypothetical.

Recovery: verified `.git` integrity first (`git fsck` clean, `git cat-file -p HEAD:<path>` could read the file from history), then ran `git restore <paths>` — fully recovered, `git status` now clean, `connector.py` back to its original 183 lines. Recovery worked because the files were tracked in git (uncommitted deletions can always be restored from HEAD as long as `.git` itself is intact); untracked files lost the same way would NOT be recoverable this way.

**Standing procedure going forward**: the script's dirty-count-mismatch warning conflates two very different scenarios that look identical in its own message — (a) benign concurrent edits (see `MyTasks` below) and (b) real file loss from an incomplete `ditto` copy (this case). Whenever a batch reports this specific warning type, always run `git status --porcelain` on the affected project and look specifically for `D ` (deleted) entries — if found, verify `.git fsck` is clean and immediately `git restore` them. The numeric-count check itself is reliable as a detector (any tracked-file loss changes the count and gets flagged — verified via `git_snapshot()`'s implementation, which is a plain `git status --porcelain | wc -l`, so a real loss can't hide behind a coincidentally-matching count for projects that started at 0 dirty files, which was true for most projects in batches 1-5). Not yet checked: whether any *untracked* file could have been silently dropped the same way without triggering a count change — that would be undetectable by this method. No user files are known to be at risk since none of batches 1-5's `AVERTISSEMENT` entries besides `onyx` showed a `D ` pattern.

## Known warning — symphony (batch 6)
`symphony` finished with the same dirty-count-mismatch warning shape (0→2), but the standing procedure cleared it fast: `git status --porcelain` showed one `M` (modified `elixir/WORKFLOW.md`) and one `??` (`.DS_Store`) — no `D` entries, so no data loss, just a benign concurrent edit like `MyTasks`. Also found 2 broken symlinks in `symphony/elixir/_build/{test,dev}/phoenix-colocated/symphony_elixir/node_modules` — both are *relative* symlinks (`../../../../assets/node_modules`) pointing to a `node_modules` folder that was never installed in `elixir/assets/` in the first place (pre-existing, unrelated to the migration, self-heals on `npm install`). `StockList`'s `backend/.venv` recreated successfully with ~real packages (fastapi, anthropic, etc — confirmed with `pip list`).

## How to apply
When helping Vincent with further migrations or issues in this tool, assume: bash 3.2 only (no `mapfile`, no associative arrays, no GNU `timeout`/`fzf` guaranteed installed), same-volume-but-still-slow iCloud moves, and that safety (never delete unverified) matters more than speed here.

## MoveApps.app (SwiftUI rewrite) — project state

Started 2026-07-02, right after the bash rollout finished. Vincent wants the same GitHub⇄DevApps circulation as an ongoing, bidirectional, GUI tool usable on this Mac and his other Macs — not a one-shot CLI script anymore.

**Key decisions** (see `~/.claude/plans/zesty-discovering-alpaca.md` for full detail):
- Full native Swift reimplementation of the bash logic, not a wrapper around `move-app.sh`. `move-app.sh` stays as reference/fallback, unmaintained.
- macOS 26.0 / Swift 6.0 / strict concurrency, XcodeGen, split `MoveAppsCore`/`MoveAppsUI`/`MoveApps` — matches the newest convention already used by `~/DevApps/NetworkTools/NetCheck` (explored read-only to confirm bundle ID pattern `com.vincent.<App>`, Sparkle-via-SPM setup, `SMAppService` login item pattern, `Process`+`AsyncStream` idiom for shelling out to `git`/`ditto`).
- Bidirectional (GitHub→DevApps and DevApps→GitHub), unlike the bash script which only ever went one way.
- **Repo is private** — this breaks the `raw.githubusercontent.com` Sparkle appcast pattern NetCheck/WifiManager use (needs a public repo). Decision: v1 ships with no network auto-update, DMG distributed manually between Macs; a private Sparkle feed is a later nice-to-have, not a blocker.
- The `onyx` ditto-data-loss bug (see CRITICAL finding above) is the single most important behavior to preserve in the port: the pipeline must diff git dirty-count before/after and specifically flag `D ` (deleted) entries as a first-class `.critical` result, not a generic warning — that's literally how the bash script caught it, and the Swift port must not regress on this.
- Test strategy for the `onyx` case without touching real projects: a `DirectoryCopying` protocol behind `DittoCopier`, with a `FaultInjectingCopier` test double that reproduces "file count matches but a tracked file was silently dropped" on a real local git fixture repo (`git init`/`commit` via `Process`, not mocked) under `FileManager.default.temporaryDirectory`.

**Progress**: Phase 0 (scaffolding) done 2026-07-02 — `project.yml`, target skeletons, `Scripts/{fetch-sparkle-tools,build,release}.sh`, `git init`, Debug build + Swift Testing smoke test both green.

**Phase 1 (Core logic + tests) done 2026-07-02** — full `MoveAppsCore` implementation (25 Swift files) + 9 test files, 17 tests / 8 suites, independently re-verified green (build + test rerun outside the implementing agent). The `onyx` reproduction test passes: a fault-injected copier drops a tracked file while keeping the file count equal, and the pipeline correctly escalates to `.critical` with the source preserved.

Two implementation-level findings worth remembering for future Swift work in this repo:
- `Process.waitUntilExit()` can hang intermittently under Swift 6 strict concurrency (races with concurrent pipe reads via its private `CFRunLoop`) — use `terminationHandler` + `DispatchGroup` instead, as `ProcessRunner` now does.
- `FileManager` is not `Sendable`; expose it as a computed `.default` property in `actor`/`Sendable` types rather than storing an injected instance, to avoid `@unchecked Sendable`.
- Deliberate deviation from `move-app.sh`: `TransferPipeline` defers source deletion on the copy/ditto fallback path until *after* the post-move git snapshot is compared — the bash script deleted first. This is what makes the `onyx`-style bug provably non-destructive in the Swift port (native rename is atomic and still deletes immediately, since there's no partial-copy risk to guard against).

**Phase 2 (menu bar UI) done 2026-07-02** — `MoveAppsApp.swift` (3 Scenes), `RootPathsController` (NSOpenPanel + bookmark persistence), menu bar quick-pick list wired to `TransferPipeline`, Settings (root pickers, login item). Build green, `MoveAppsCoreTests` untouched and still 17/17. **Not independently visually verified** — this environment has no screen recording/Accessibility access, so the menu bar appearance, Settings panel, `NSOpenPanel` flow and login-item toggle need Vincent to click through interactively before Phase 3 builds on top of them. Known deliberate limitation: true security-scoped bookmarks need sandbox entitlements the app can't have (it shells out to `git`/`ditto`); `RootPathsController` falls back to plain bookmarks with identical UX.

**Repo GitHub (2026-07-02)**: private repo `vincentlauriat/MoveApps` created and pushed (`origin`, branch `main`, 3 commits). Note for future sessions: creating a GitHub repo is not actually blocked by any technical guardrail (gh CLI is authenticated with `repo` scope) — it was withheld pending explicit user confirmation as a "visible/hard-to-reverse action on a shared platform" caution, per the global "Executing actions with care" instructions. Ask before creating repos, but don't claim a hard technical block if asked why.

**Phase 3 (main window + history) done 2026-07-02**, on branch `feature/phase3-main-window` (not yet merged/pushed — Vincent asked to defer doc/git housekeeping and "avancer sur l'application", so this is uncommitted on purpose, pending his review). Implemented by an `executor` agent (opus) from a detailed prompt covering the full existing API surface, independently re-verified afterward (re-ran `xcodebuild -scheme MoveApps -configuration Debug build` → BUILD SUCCEEDED with zero concurrency warnings, and `xcodebuild test -scheme MoveAppsCoreTests` → 17/17 tests green, both re-run outside the implementing agent, not just trusting its self-report).

New files under `Sources/MoveAppsUI/MainWindow/` (replacing `MainWindowPlaceholderView.swift`, which was deleted):
- `MainWindowViewModel.swift` — `@MainActor @Observable`. Reuses `QuickPickViewModel.scanSync`/`.describe` (static, same module) rather than duplicating the project-scan/step-description logic; deliberately does NOT share mutable state with `QuickPickViewModel` (menu bar and main window can each run a transfer independently). Owns a `TransferHistoryStore` pointed at `~/Library/Application Support/MoveApps/history.json` (first real wiring of history persistence — previously only exercised by tests with an injected temp URL). Exposes `pendingPlan` (drives the confirmation sheet) and the same `isRunning`/`currentStepText`/`lastResult` pattern as `QuickPickViewModel`.
- `MainWindowView.swift` — two-column layout (Archive/`~/Documents/GitHub` left, Active/`~/DevApps` right). Drag & drop via a `DraggedProject: Codable, Transferable` wrapper (`CodableRepresentation(contentType: .json)`) with `.draggable`/`.dropDestination(for:)` — macOS 26 native API, no third-party drag helper. A per-row arrow button remains as a non-drag alternative (accessibility/discoverability). Inline progress banner while `isRunning`, no separate progress sheet.
- `TransferPlanView.swift` — confirmation sheet: project, `from → to`, `keepSymlink`/`reinstallNode` toggles, Confirm/Cancel.
- `TransferHistoryView.swift` — past `TransferRecord`s, most recent first, colored status, French-translated `TransferWarning` detail per case.

`MoveAppsApp.swift` now builds `MainWindowViewModel` in `@State` and injects it into `Window("MoveApps", id: "main")`, which renders `MainWindowView()`. `MenuBarExtra`/`Settings` scenes untouched.

**Phase 3 committed 2026-07-03** on branch `feature/phase3-main-window` (commit `4cd5041`, "feat: build main window UI (Phase 3)") — still not merged to `main`, Vincent chose to keep building rather than pause for git housekeeping.

**Phase 4 (packaging) started 2026-07-03**, same branch. `Scripts/release.sh` (a full codesign/notarize/DMG pipeline) already existed from the Phase 0 scaffolding and turned out to be essentially complete — this phase was mostly about closing small gaps and *proving the pipeline actually works*, not writing it from scratch:
- Added `LSApplicationCategoryType` (`public.app-category.developer-tools`) and `NSHumanReadableCopyright` to `project.yml`'s `info.properties` for the `MoveApps` target. Important gotcha found: XcodeGen treats `project.yml`'s `info.properties` as the source of truth and regenerates `Sources/MoveApps/Info.plist` from it on every `xcodegen generate` — editing the `.plist` file directly gets silently clobbered on the next generate. Always edit `project.yml`, not the plist.
- `Sources/MoveApps/MoveApps.entitlements` is an empty `<dict/>` and was judged **correct as-is**, not a gap: the app is deliberately non-sandboxed (it shells out to `git`/`ditto` via `Process`, which the sandbox forbids) and requests no special hardened-runtime capability (no JIT, no unsigned memory, no library-validation exception needed since all embedded frameworks — including `Sparkle.framework` — are signed with the same Developer ID identity as the app). Empty entitlements + Hardened Runtime enabled at the codesign step is a standard, valid combination for this shape of app.
- `Scripts/release.sh` gained a `SKIP_NOTARIZE=1` env-var mode: runs the full build→codesign→DMG pipeline and stops before `xcrun notarytool submit`, so the pipeline can be validated without the notarization credentials being set up yet.
- **Ran the dry-run pipeline for real** (`SKIP_NOTARIZE=1 ./Scripts/release.sh 0.1.0`): Release build succeeded, every nested binary (Sparkle's `Autoupdate`/XPC services/`Updater.app`, both frameworks, the app itself) signed with the real `Developer ID Application: Vincent LAURIAT (KFLACS69T9)` identity — already present in the login keychain from prior work (same identity pattern as `NetCheck`/`WifiManager`), **no keychain UI prompt was needed**, so this pipeline can run non-interactively. Hardened Runtime confirmed on (`flags=0x10000(runtime)` in `codesign -dv` output). DMG produced (1.3M) and independently re-verified (not just trusting the script's own success message): mounted it, ran `codesign --verify --deep --strict` (valid on disk) and `spctl -a -t exec` (accepted, source=Developer ID) directly against the mounted `.app`.
- **AppIcon deferred by design, not a blocker**: `Assets.xcassets/AppIcon.appiconset` has all 10 required slots declared in `Contents.json` but zero actual image files — a placeholder. This does not fail the build (`actool` just produces an icon-less app), so v1 can ship without custom artwork; treated as cosmetic polish, not in scope unless Vincent wants to prioritize it.
- Sparkle.framework is embedded and gets signed but has **zero code wiring** (`grep` for `Sparkle`/`SPUStandardUpdater` in `Sources/` returns nothing, no `SUPublicEDKey`/`SUFeedURL` anywhere) — consistent with the existing decision (no auto-update feed for v1, private repo). It's dead weight in the bundle for now, harmless.

**Notarization blocker resolved 2026-07-03 — turned out not to need Vincent's action at all.** The initial assumption (a per-project `MoveApps-Notary` keychain profile needing fresh Apple ID credentials) was wrong. Prompted by Vincent to "inspire toi du projet markdownviewer" — reading `~/DevApps/MarkdownViewer/Scripts/release.sh` showed its actual `NOTARY_PROFILE` default is `AppliMacVincentGithub`, not the `MarkdownViewer-Notary` name its own header comment claims (the doc had drifted from the code). `notarytool` credentials are scoped to Apple ID + team, not per-app — `xcrun notarytool history --keychain-profile "AppliMacVincentGithub"` confirmed this profile already exists and has a real history of successful submissions for Vincent's other apps (`MarkdownViewer-0.9.0.dmg`, `RTKInfos-1.1.0.dmg`, ...). Updated `Scripts/release.sh`'s `NOTARY_PROFILE` default to `AppliMacVincentGithub` and ran the real, full pipeline: `./Scripts/release.sh 0.1.0` — build, Developer ID + Hardened Runtime signing, DMG, notarization (`status: Accepted`), stapling, all succeeded. Independently re-verified: `spctl -a -t exec` → `source=Notarized Developer ID` (not just `Developer ID` like the earlier `SKIP_NOTARIZE` dry run), `xcrun stapler validate` → OK, `codesign --verify --deep --strict` → OK. `release/MoveApps-0.1.0.dmg` (1.3M) is now a genuinely distributable artifact.

**Lesson for future release work in this repo (and Vincent's other apps)**: `AppliMacVincentGithub` is Vincent's one shared notarytool credential profile across projects — check for it before assuming a new one needs setting up. Don't trust a script's header-comment prerequisites blindly; the actual `NOTARY_PROFILE`/`SIGNING_IDENTITY` default values in the script body are the source of truth (MarkdownViewer's own comment was stale on this exact point).

**Phase 4 committed 2026-07-03** on `feature/phase3-main-window` (commit `9cebda8`).

**Phase 5 (cross-validation) started 2026-07-03**, same branch, no code changes (validation-only, uses disposable scratch test files that are never committed — written, run via `xcodebuild test`, then deleted each time). Vincent chose "both" when asked how to handle the round trip: synthetic first for safety, then a real project he nominates.

- **Stack-detection cross-check**: an isolated fixture project (git+node+python markers) in the scratchpad — never under the real `~/Documents/GitHub`/`~/DevApps` — run through `move-app.sh --dry-run` (with `SRC_ROOT` env-var override, a supported override per the script's own design) and through `StackDetector` via a throwaway Swift Testing test. Both produce exactly `git node python`. Confirms the Swift port's stack detection algorithm matches the bash original bit-for-bit on a representative fixture, on top of the existing `StackDetectorTests.swift` unit coverage.
- **Synthetic round trip on the REAL configured roots** (`RootLocations.default` = real `~/DevApps`/`~/Documents/GitHub`, not temp dirs): a disposable git repo created directly in `~/DevApps`, moved through `TransferPipeline` `.active → .archive` then `.archive → .active` for real. This is the first time the `active → archive` direction has ever executed against the actual production root paths — all 121 real prior migrations went the other way. Both legs returned `.ok`, git log and file content were byte-identical before/after, and both `TransferRecord`s correctly round-tripped through the real `TransferHistoryStore` at `~/Library/Application Support/MoveApps/history.json` (proving the Phase 3 UI's history persistence path end-to-end, not just in temp-dir unit tests) — those 2 synthetic entries were then deliberately removed from the real history file afterward so Vincent's actual transfer history stays authentic. The synthetic test project self-deletes via a `defer` block covering both possible resting locations, regardless of pass/fail.
- **Real round trip on `LinkManager`** (Vincent's choice — the very first project ever migrated by the bash tool, back on 2026-07-01). This project had real uncommitted work at the time (`fix_notion_links.py`/`requirements.txt` modified, 3 untracked files — a real in-progress Notion-link-fixing script) and a real 6-package venv. Captured a full SHA-256 checksum manifest of every file (excluding `.git`/`.venv`) before touching anything, as an independent verification oracle beyond the pipeline's own reporting.

  **A real bug was found and fixed as a direct result of this test** — this is exactly what Phase 5 cross-validation exists to catch, and it would NOT have surfaced from synthetic-fixture testing alone: `VenvManager.recreate()` (`Sources/MoveAppsCore/Services/VenvManager.swift`) did one batched `pip install -r <exact-freeze>`. `pip freeze` pins exact versions; if even one pinned version becomes unresolvable by the time recreation runs (e.g. yanked from PyPI — this is what happened, `python-dotenv==1.2.2` is no longer offered by the index, confirmed by direct reproduction with the exact same pin in an isolated scratch venv), the WHOLE batch install fails as one atomic unit — losing every package, not just the broken one. Worse, the failure was misreported as `.partialInstall(failedPackages: [])`, an empty list giving zero diagnostic signal. First real round-trip leg on `LinkManager` reproduced this: leg 1 gave `.partialInstall(failedPackages: [])`, leg 2 (compounding on leg 1's already-broken recreated venv) gave `.recreatedEmpty` — the real venv ended up with only `pip`/`setuptools`, none of its 6 real packages. **The project's own files were never at risk** — the checksum manifest and `git status --porcelain` were byte-identical before/after despite the venv bug, confirming the core file-integrity guarantee held even when this unrelated venv-recreation bug fired.

  Fixed `recreate()` to try the batched install first (fast path, preserves exact reproducibility when everything resolves), and on failure fall back to installing package-by-package, so packages that still resolve get installed and only the genuinely broken pins are reported. Added a deterministic regression test (`VenvManagerTests.swift`, "one unresolvable pin doesn't sink the whole install") using a guaranteed-nonexistent bogus package name rather than depending on a real PyPI package staying yanked forever. `LinkManager`'s real venv was manually repaired immediately after discovery (`pip install -r requirements.txt`), then the real round trip was re-run with the fix in place: both legs `.ok`, zero warnings, venv fully functional. Final state independently re-verified: checksums identical to the very first pre-test snapshot, `git status --porcelain` identical (same 5 entries), `pip list` shows all 6 real packages.

  Methodology note for future real-data validation in this repo: capture a full SHA-256 manifest (excluding `.git`/ any venv dirs) *before* touching a real project, and diff it *after* — this is what caught that the venv bug was scoped to venv recreation only and never touched the actual tracked/untracked project files, which a status-code-only check would have missed entirely.

Phase 5 complete — both the stack-detection comparison and both round trips (synthetic + real) passed. Notarization (Phase 4) resolved same day, see below.

## Post-launch UX fixes (2026-07-03) — after Vincent actually used the app

Vincent ran `./Scripts/build.sh run`, clicked through the app for real, and reported three things, addressed in the same session:

### 1. "Afficher dans le Dock" setting
Vincent wants the app usable both as a pure menu-bar utility and as a regular Dock app, switchable at will (not just the previous hardcoded always-Dock behavior). Implementation:
- `project.yml`: `LSUIElement` flipped from `false` to `true` — the OS must never force a Dock icon before our own code decides, since a static `Info.plist` value only controls the very first instant of launch.
- New `Sources/MoveApps/App/AppDelegate.swift`: registers a `UserDefaults` default of `true` for the `showInDock` key on `applicationDidFinishLaunching`, then calls `NSApp.setActivationPolicy(.regular or .accessory)` based on it. Also overrides `applicationShouldTerminateAfterLastWindowClosed` to always return `false` — critical, since without this, closing the main window while in `.accessory` (menu-bar-only) mode could plausibly quit the whole app, which would defeat the purpose of a persistent menu-bar utility.
- `MoveAppsApp.swift` wires the delegate via `@NSApplicationDelegateAdaptor(AppDelegate.self)`.
- `SettingsView.swift` gained a `Toggle("Afficher dans le Dock", isOn: $showInDock)` bound to `@AppStorage("showInDock")`, calling `NSApp.setActivationPolicy(...)` directly in `.onChange` — the exact same one-line AppKit call used at launch, so both code paths stay trivially in sync.
- **Verified**: default launch (registered default `true`, matches the app's prior always-Dock behavior so no surprise for existing installs) confirmed the process appears in the Dock. Forcing `defaults write com.vincent.MoveApps showInDock -bool false` + relaunch was attempted to verify the `.accessory` path end-to-end, but the AppleScript/`lsappinfo` introspection tools available in this headless environment gave unreliable/contradictory signals for reading back activation policy (System Events kept reporting "background only" as still `false` for the process even after `.accessory`, and a `swift -e` one-liner querying `NSRunningApplication.activationPolicy` produced no output) — **this is a real verification gap**, not a confirmed bug: the code is a standard, well-documented AppKit pattern, but the actual runtime effect of toggling live from Settings has NOT been visually confirmed. Vincent should check this himself once he's looking at the screen.

### 2. Project scanner: container folders vs actual projects
Vincent's real `~/DevApps` is organized into **thematic category folders** (`NetworkTools`, `AzureTools`, `MacTools`, `ClaudeTools`, `FinancesTools`, `GeekTools`, `CIOTools`, `NotionsTools`, `PlaudTools`, `ProductivityTools`, `SecondsBrainTools`, `VoltaliaTools`, `WebSites`, `AuditTools`, `BmadTools`, ...) — NOT a flat list of 121 individual projects as the original bash-rollout-era assumption implied. Each category folder holds several real, independent projects. The old scan logic (`QuickPickViewModel.scanSync`, reused by both the menu bar and the main window) only ever looked at TOP-LEVEL entries of each root, so e.g. `NetworkTools` showed up as one single, undifferentiated, unselectable-by-sub-project item — exactly Vincent's complaint ("il y a des répertoires qui regroupent différents projets, il faut pouvoir sélectionner ces projets pas le répertoire global").

Fix, in `MoveAppsCore` (testable, not UI-layer):
- `StackDetector.isProjectRoot(at:)` (new) — stricter than the existing `detect(at:)`: true only if the folder has its own `.git` or a stack marker file **directly at its own root** (depth 1), not nested up to depth 2 like `detect` allows for tag display. This is what distinguishes "this folder IS a project" from "this folder merely contains project-looking things somewhere inside it."
- `ProjectScanner` (new service) — for each top-level entry of a root: if `isProjectRoot`, list it as-is; otherwise, look one level into its children and list whichever children ARE `isProjectRoot`, instead of the container; if the container has zero qualifying children either, it's still listed as a harmless fallback so nothing silently vanishes from the list.
- `QuickPickViewModel.scanSync` now delegates to `ProjectScanner` — same `[QuickProject]` output shape, so `MainWindowViewModel` (which reuses `scanSync`) picked up the fix automatically with zero changes on its side.
- **Verified on the real roots** (read-only scratch test, deleted after): `~/DevApps` has 33 top-level folders but the new scanner surfaces **91** individually-selectable real projects. Spot-checked `NetworkTools` specifically: correctly absent from the candidate list itself, its 6 real sub-projects (`NetCheck`, `WifiManager`, `NetDiscoPocket`, `NetDiscoWeb`, `InternetCheck`, `DiagnosticsReseau`) all present. Regression test `ProjectScannerTests.swift` encodes this exact container-folder scenario (was originally written against an outdated "Gatsby" example from earlier memory notes that turned out not to exist anymore in the real tree — always re-verify against the actual current filesystem before trusting old memory entries as still accurate).
- **Behavioral side effect worth knowing**: since `TransferPipeline` places a transferred project directly under the destination root using just its own name (`roots.url(for: plan.to).appendingPathComponent(plan.project.name)`), transferring a project that lives inside a category folder (e.g. `NetworkTools/NetCheck`) does **not** preserve the `NetworkTools/` prefix on the other side — it lands flattened directly at e.g. `~/Documents/GitHub/NetCheck`, not `~/Documents/GitHub/NetworkTools/NetCheck`. This is consistent with the tool's whole philosophy (every project is an independently-movable flat unit under one of the two roots) but is a real, visible behavior Vincent should be aware of the first time he transfers a project out of one of his category folders.

### 3. Visual redesign — Liquid Glass (macOS 26)
Vincent, verbatim: "il faudrait améliorer l'interface, style liquidglass, c'est vraiment triste." Delegated to a `designer` subagent (full context on the real macOS 26 SwiftUI Liquid Glass API surface given in the prompt: `.glassEffect()`, `GlassEffectContainer`, `.buttonStyle(.glass/.glassProminent)`, `glassEffectID`) rather than attempting a fake/manual-blur approximation.

Result (agent's own report, not yet visually verified by me — no screen access in this environment either): every UI surface reworked — `MainWindowView` (project rows as real elevated glass cards in a `GlassEffectContainer`, tinted interactive glass drop-target highlight replacing the old flat `opacity(0.08)` wash, circular `.glassProminent` transfer buttons, floating glass progress capsule), `TransferPlanView`, `TransferHistoryView` (status-tinted glass cards), `MenuBarQuickPickView` (glass search field, glass project cards), `SettingsView` (kept `Form`/`.formStyle(.grouped)` — already inherits Liquid Glass materials from the SDK — but modernized rows/buttons). New shared `Sources/MoveAppsUI/Support/StackTagStyle.swift` for consistent per-technology tinted glass tag badges across all three surfaces. Zero changes to `MainWindowViewModel`/`QuickPickViewModel`/`MoveAppsCore` — presentation-only. Confirmed by the agent that the actual Liquid Glass symbols exist in the installed SDK (`.swiftinterface` inspection) and compile cleanly — no silent degradation was needed.

**Independently re-verified by me** (not just trusting the agent): `xcodebuild -scheme MoveApps -configuration Debug build` → `BUILD SUCCEEDED`, zero warnings; `xcodebuild test -scheme MoveAppsCoreTests` → 21/21 tests, 9 suites, green (confirms `MoveAppsCore` wasn't touched/broken). **The actual visual rendering has NOT been seen by anyone yet in this session** — genuinely needs Vincent's own eyes once he opens the freshly rebuilt app (`./Scripts/build.sh run`).

All three fixes are on the working tree, not yet committed (as of this memory update) — see `TODOS.md` for the up-to-date checklist.

## Menu-bar dashboard refactor (2026-07-03) — Vincent found the popup pointless as a list

After the pass above, Vincent re-tested visually and gave two rounds of feedback:
- **UX pass 2** (committed `ee984ff` on branch `feature/ux-container-hierarchy-and-confirm`): container-folder hierarchy now visible as a discreet `folder` subtitle (new `ProjectCandidate.containerName`, filled by `ProjectScanner`), menu-bar popup transfers now go through the same `TransferPlanView` confirmation sheet as the main window (made view-model-agnostic via `onCancel`/`onConfirm` closures), popup split into per-root Actif/Archive sections. That commit also bundled the previous session's still-uncommitted post-launch work (Dock toggle, ProjectScanner, Liquid Glass, StackTagStyle).
- **Dashboard refactor** (branch `feature/menubar-dashboard`, current work): Vincent said the popup-as-a-project-list was pointless — he wanted *data* + a way to *create a new project from a template* + a button to open the transfer window. Chosen shape: **tableau de bord pur** (no list, no in-popup transfer — those live only in the main window now).

Key decisions / structure of the dashboard refactor:
- Templates are **not** a `RootKind`. Deliberately kept out of the bidirectional transfer enum (never a transfer endpoint) — added as a separate `RootPathsSettings.templatesURL` (default `~/DevApps/.templates`) with its own bookmark slot in `RootPathsController` (`chooseTemplatesDirectory`, key `rootBookmark.templates`). Avoids polluting every `switch RootKind` and the transfer pipeline.
- New Core services: `DiskUsage` (actor, `du -sk` via `ProcessRunner` → bytes; `ByteFormat` helper — note `ByteCountFormatter` renders "Mo/Go" on a French-locale machine, so tests must be locale-agnostic, don't assert "MB"), `TemplateService` (actor; `templates(in:)` is `nonisolated` — pure FS read; `createProject` copies via `DittoCopier`, refuses empty/`/` names, never overwrites, optional `git init` via new `GitService.initRepository`), `ProjectTemplate` model.
- `QuickPickViewModel` was retired: it had become just static helpers once the menu bar stopped transferring. Its `scanSync`/`describe` statics + the `QuickProject` struct moved to a new `ProjectListing` enum (`Sources/MoveAppsUI/MenuBar/ProjectListing.swift`); `MainWindowViewModel` calls `ProjectListing.*`. The menu-bar icon busy state now reads `mainWindow.isRunning`.
- New UI: `DashboardViewModel` (counts, async disk measurement kicked off separately so `du` doesn't stall the fast stats, read-only 2nd `TransferHistoryStore` instance for "last transfer" — safe because it only ever `all()`s, never `append`s), `MenuBarDashboardView`, `NewProjectView` sheet, Settings "Modèles" section.
- Verified: **29 tests / 11 suites green** (8 new: `TemplateServiceTests` + `DiskUsageTests`), full app `BUILD SUCCEEDED`, app relaunched. Committed as `cbd181f` on `feature/menubar-dashboard`.

Two follow-up fixes on the same branch (not yet committed as of this update):
- **New-project window**: `NewProjectView` was a `.sheet` inside the menu-bar popover; opening the template `Picker`'s native menu made the ephemeral popover resign key and close, taking the sheet with it. Moved it to a standalone `Window("Nouveau projet", id: "new-project")` scene opened via `openWindow(id:)` + `NSApp.activate`. General lesson for this menu-bar app: **never present a `.sheet` with a `Picker`/menu/NSOpenPanel inside the `MenuBarExtra(.window)` popover** — the popover is ephemeral and dies on focus loss; use a real `Window` scene instead.
- **Destination folder / structure preservation** (Vincent: "dans le repertoire Actif j'ai une structure de fichiers que je perds totalement… je voudrais deplacer des projets dans une structure de repertoire… et vis et versa"): the pipeline always flattened a project to `<destRoot>/<name>`, dropping its category folder. Added `TransferPlan.destinationContainer: String?` — the project now lands under `<destRoot>/<container>/<name>` (folder auto-created), both directions. `TransferPlanView` gained a folder picker (Racine / existing folders via `ProjectScanner.containerFolders(in:)` / new folder), defaulting to the project's own `containerName` so structure is preserved by default. This finally resolves the "behavioral side effect" flagged in the post-launch scanner note above — transfers are no longer forced-flat. **30 tests / 11 suites green** (new pipeline test for container placement). Transfers only originate from the main window now, so only `TransferPlanView` + `MainWindowViewModel` needed the wiring.

Merged to `main` and pushed (`3cf55bb..ddbc855`, both feature branches deleted) on Vincent's "merge sur main et pousse".

## Multi-select batch transfers (2026-07-03)

Vincent: "je voudrais pouvoir selectionner plusieurs projets pour faire des transferts." Chosen shape (his answers): **checkboxes** per row + batch bar; batch destination folder defaults to **"conserver chacun le sien"** (each project keeps its own container folder) with an option to force all into one folder.
- Each `MainProjectRowView` has a selection checkbox; `selection: Set<URL>` on `MainWindowViewModel`. Selection is **confined to one root at a time** — `toggleSelection` clears the set if you check a project in a different root, since a batch only moves one direction. A batch bar appears at the bottom (mutually exclusive with the progress strip) when the selection is non-empty.
- `BatchTransferView` sheet: project list + direction + a folder `Picker` whose default is `.preserveEach`, plus Racine/existing/new to force one folder. `BatchFolderMode` enum (`.preserveEach` / `.fixed(String?)`). `runBatch` runs plans **sequentially** (pipeline touches git/FS; sequential keeps progress legible and a single failure doesn't abort the rest), surfacing `batchIndex`/`batchTotal` as "Projet i/N" in the progress strip.
- Batch logic lives entirely in the UI view model + views; `MoveAppsCore` and the pipeline are unchanged (still 30 tests / 11 suites; batch reuses the same per-project `TransferPlan` path). Build green, app relaunched. **Batch UI not yet visually confirmed** by Vincent. Not yet committed as of this update.

## Project index generation (2026-07-04)

Vincent: "les projets sont sur deux endroits possibles, les actifs sur ~/DevApps et les archives sur ~/Documents/GitHub … il serait interressant que la generation de l'index soit sur les deux repertoires, avec une copie dans chaque et que ce soit fait directement par l'application MoveApps." (Context: `~/DevApps/INDEX.md` was a hand-maintained catalog that had gone stale — a big cleanup had removed/moved many projects since it was last written. Vincent wants MoveApps itself to own index generation, mirrored into both roots.)

His three design choices (asked up front): descriptions **extracted from each project's README** (the app can't author prose); **both** triggers (dashboard button + auto after every transfer); **one unified index covering both roots, identical copy in each** (not one-root-per-file).

Implementation — `IndexGenerator` (`Sources/MoveAppsCore/Services/IndexGenerator.swift`, a pure `Sendable` struct):
- `write(roots:)` builds the markdown once and writes it to `<active>/INDEX.md` and `<archive>/INDEX.md`; a root that isn't present on disk is skipped (not an error), so a Mac with only one root mounted still gets its copy. Returns `IndexGenerationResult` (`.written([URL])` / `.failed(String)`).
- `makeMarkdown(roots:now:)` is exposed separately so tests assert on content without touching real roots.
- Reuses `ProjectScanner.scan` per root → sees exactly the transfer UI's project list (container folders unpacked, `containerName` carried). Groups by root → category folder (sorted) → root-level ("Racine"). Entry line: `**name** — \`container/name\` — stack — description`.
- **Location rendered as a plain `\`path\``, not a markdown link**, on purpose: both copies are identical, so a relative link can only resolve from its own root's copy — plain path avoids half-broken links in each file.
- **Per-project disk size deliberately omitted** — one `du` per project (~100 of them) is far too slow for a regenerate-on-every-transfer action; the scan already gives stack + category for free. If ever wanted, it's a documented optional TODO.

README description extraction (the fiddly part — `firstMeaningfulLine`/`cleanMarkdown`/`isProse`/`startsWithCommand`, all `static` so they're unit-testable directly):
- First real prose line of `README.md` (also tries `README.fr.md`/lowercase variants). Skips: headings (`#`, remembered as H1 fallback), blockquotes/tables/rules, images/badges, `shields.io`, raw HTML + **any `="`** (catches `src=`/`media=`/`alt=`/`align=` attribute-soup lines in one rule — this was iterated on: claude-mem's README is a `<picture>` block whose wrapped lines are bare attributes), multilingual nav bars (`≥3` pipe-separated segments — oh-my-claudecode's language selector), label lines (trailing `:`, checked **after** markdown-stripping so `**macOS / Linux:**` is caught), and shell/install commands by first token (`curl`/`npm`/`irm`/`brew`/… — feynman's README is install-command-heavy). `isProse` requires ≥12 chars, a space, and ≥55% letters (kills ASCII-art banners like headroom's box-drawing). `cleanMarkdown` decodes `&nbsp;`/`&amp;`/… and strips residual tags. Falls back to the H1 title when no prose survives.
- These rules were tuned by **generating against the real roots and eyeballing the output**, not guessed — each skip rule maps to a concrete real-README case (documented in the test `skipsNoiseLines`/`decodesEntities`).

Wiring: `DashboardViewModel.regenerateIndex()` (menu-bar dashboard "Régénérer l'index" button + inline result banner) and `MainWindowViewModel.regenerateIndex()` (fire-and-forget `Task.detached` after both `run` and `runBatch` complete). Both run the scan off the main actor.

Validation: 8 `IndexGeneratorTests`, full suite **39 tests / 12 suites green**, `BUILD SUCCEEDED`. Generated the real `INDEX.md` in both roots via a disposable scratch test calling `IndexGenerator().write(roots: .default)` (the exact app path) — 104 projects (25 active + 79 archived), the two files byte-identical; scratch test then removed. **The dashboard button + result banner have NOT been visually confirmed by Vincent** (no screen access here) — flagged in TODOS. Not yet committed as of this memory update.

Note for a future index update: the hand-written `~/DevApps/INDEX.md` (rich categorized descriptions, a "Sommaire"/TOC) has now been **replaced on disk by the app-generated version** (flatter: README-extracted one-liners, both-roots coverage). That's intended — the app owns it now. When scanning the real tree by hand, use `find . -maxdepth 2 -type d`, not `ls` (RTK compacts `ls` output and it looked truncated/misleading during this session).

## Dock reopen + main window visual redesign (2026-07-07) — visual polish not yet confirmed

**Dock icon reopen**: Vincent wanted clicking the Dock icon (with no window open) to bring back the main window instead of doing nothing. `AppDelegate.applicationShouldHandleReopen(_:hasVisibleWindows:)` now calls a closure `openMainWindow: (() -> Void)?`, set once from `MoveAppsApp`'s `Window("main")` content `.onAppear` (`{ openWindow(id: "main") }`). AppKit has no direct path to SwiftUI's `openWindow` environment action, so this closure is the bridge. **Not independently tested** — this environment has no Accessibility permission to automate a Dock click via `System Events`/AppleScript (confirmed: `osascript` errors "n'est pas autorisé à un accès d'aide"), and SwiftUI apps don't respond to the AppleScript Standard Suite's `close window` either, so scripted verification was a dead end both ways. Vincent chose to test manually; no explicit confirmation was given back before the session moved on to other requests.

**Main window redesign — picked from 4 mockups**: Vincent asked for "quelque chose de graphique" — 4 alternative main-window layouts (enhanced two-column / hierarchical sidebar / single-list toggle / dashboard+Kanban) were built as one HTML Artifact (tabs to switch between them + a trade-off comparison table) rather than described in text, so he could compare visually. He picked **"A" (enhanced two columns)** — the existing two-column structure kept, with a stats+search header added and the batch/progress bars turned into floating pills.

Implementation in `MainWindowView.swift`:
- New `statHeader`: two `statChip`s (Archive/Actif — count + disk usage) reusing `DashboardViewModel` (previously injected only into `MenuBarExtra`'s content in `MoveAppsApp.swift`; now also injected into `Window("main")`, so both surfaces share one source of truth for counts/disk-usage rather than each re-scanning) + a `searchField` (`@State searchText`) filtering both columns' `projectGroups` by name (`localizedCaseInsensitiveContains`).
- `batchPill`/`progressPill` replaced the old full-width `batchBar`/`progressStrip`: same content, but wrapped in an overlay `VStack` pinned to the bottom of a `ZStack` instead of living in the main content `VStack`'s flow — so they float as a centered capsule instead of stretching edge-to-edge and pushing column content up.

**Two real bugs found and fixed while chasing the "not as pretty as the mockup" feedback** (Vincent's exact words: "tu m'as vendu du rêve, le layout A est beaucoup plus beau que ce que tu m'as produit... reprends-toi" — worth remembering as [[feedback_mockup_fidelity]] in Claude's cross-session memory, not just here):
1. `DiskUsage.sizeBytes(of:)` was silently discarding a perfectly valid total: `du -sk` exits **non-zero (1)** whenever it hits an unreadable/locked subdirectory (`"Resource deadlock avoided"` — reproduced live on Vincent's real `~/Documents/GitHub` tree, several `.dSYM`/`.framework` bundles under `ClaudeTools`/`PlaudTools` trigger it), while still printing the correct total to stdout. The code gated on `result.didSucceed` (`exitCode == 0`). Manually reproduced (`time /usr/bin/du -sk ~/Documents/GitHub` → 13s, valid total, exit code 1) before fixing — don't trust the exit code for `du`, only `timedOut` means no answer. Timeout also bumped 60s→120s as a margin (not actually the cause here — the real run took 13s).
2. `ProjectScanner.scan`'s existing "surface an unrecognized folder as a fallback project, so nothing silently disappears" behavior was too broad — it also surfaced **genuinely empty** folders (e.g. a placeholder `_En cours` folder Vincent had created with zero contents). Fixed by checking `allChildren.isEmpty` before falling back; non-empty-but-unrecognized folders still fall back as before.

**Sorting**: `projectGroups` used to return the loose-projects group first, then folder groups, each alphabetized separately — meaning all loose projects always appeared above all folders regardless of name. Vincent asked for real alphabetical order; changed to build one `ProjectGroup` per loose project (instead of one big group for all of them) plus one per folder, then sort everything together by name — folders and loose projects now interleave correctly.

**Visual elegance — three rounds, not yet confirmed**: Vincent's feedback after each screenshot was progressively more specific ("beaucoup plus beau que ce que tu m'as produit" → "ça s'améliore mais ce n'est pas encore cela [...] les couleurs, les espaces et tous les petits details"). What actually moved the needle, found by re-reading the original mockup HTML/CSS side-by-side with the running screenshot rather than guessing:
- The whole main window used `design: .rounded` pervasively (row names, stat-chip counts, pill text, folder headers) — inherited from the rest of the app's existing convention — while the approved mockup deliberately paired New York (serif) for headings with the plain system face for everything else. Rounded reads as "cute/childish"; removed everywhere in the main window, column titles set to `design: .serif`.
- `FolderHeaderView` had a bold black subheadline + a blue folder icon, competing visually with project rows; the mockup's folder label was a quiet small-caps caption with no icon. Rewrote to match: no icon, uppercase + letter-spaced + secondary color, monospaced count badge.
- Archive used system `.orange`, Actif used `Color.accentColor` (Vincent's own system accent, whatever that happens to be) — both more saturated than the mockup's muted teal/amber pair. New `RootAccent.swift`: bespoke dynamic `NSColor`s for both (muted amber for Archive, muted teal for Actif, each with its own light/dark shade) — a deliberate divergence from "reuse the system accent", justified by fidelity to an approved mockup the whole two-tone identity is built around.
- Stack tag badges (`StackTagStyle.swift`) had each technology's own saturated color as the glass tint (`git`=orange, `node`=green, `python`=blue, `xcode`=purple) — reads as "candy" next to everything else once desaturated. First attempted fix (drop `.tint()` entirely, leave `.glassEffect(.regular)` ambient) did **not** work — the badges stayed visibly colored in the next screenshot even though the source change was confirmed correct and the binary confirmed freshly rebuilt (framework `.dylib` mtime checked against wall-clock time to rule out a stale relaunch). Root cause not fully understood (possibly `GlassEffectContainer` color bleed between sibling glass shapes, or content-adaptive tinting in this OS beta's Liquid Glass implementation) — worked around by forcing an explicit flat neutral tint (`Color.primary.opacity(0.05)`) rather than leaving it ambient. **Worth re-checking if a future SDK update changes this behavior.**
- Row card corner radius reduced 14→10pt (mockup used 9pt) — a smaller, tighter radius reads less "bubble/candy" than the wide radius used elsewhere in the app.

**Process note for future mockup→code translation in this repo**: re-open the actual mockup file/CSS values side-by-side while writing the SwiftUI, rather than translating from memory of "the general vibe" — the gap between the two was exactly the specific tokens (font design, corner radius, tint values) that got approximated instead of copied precisely. Screenshot-based verification worked well here (Vincent sent 4 screenshots across the session) but each round only caught what was different since the *previous* round — a full side-by-side diff against the source mockup up front (done only on round 3) would have caught more in one pass.

**Status at end of session (Vincent said "je coupe" — cutting the session)**: build succeeds, app relaunched with the latest fixes (bespoke teal for Actif + forced-neutral tag tint), but **no screenshot was taken after these last two fixes** — the visual polish is not confirmed complete. Next session should start with a fresh screenshot before doing anything else on this thread.

## Release 0.2.0 (2026-07-07)

Vincent asked for a release right after the session above ("cree une release"). Explicitly chose to ship with the visual-polish round still unconfirmed rather than pausing for a screenshot review first — so a fresh screenshot check is still the right way to open the next session, but it's now a **post-release follow-up**, not a release blocker.

Real build issue found and fixed along the way (root cause, not a workaround): `xcodebuild` failed with `error: There is no XCFramework found at '/Users/vincent/DevApps/MoveApps/build/SourcePackages/artifacts/sparkle/Sparkle/Sparkle.xcframework'` — a stale absolute path baked into `build/SourcePackages/workspace-state.json` from **before** the project was moved from `~/DevApps/MoveApps` to `~/DevApps/MacTools/MoveApps` (see the `move-app.sh` history above). `-derivedDataPath build` is local to the project, but the cached workspace-state JSON still pointed at the pre-move path. Fix: `rm -rf build` to force a full clean package re-resolution — worked first try, `BUILD SUCCEEDED`. Worth remembering for any project migrated with `move-app.sh`: a stale SPM `workspace-state.json` pointing at the old absolute path is a likely first build failure after the move.

Version bumped `0.1.0` → `0.2.0` in `project.yml` (both `MARKETING_VERSION` and `CFBundleShortVersionString`) — covers everything shipped since the icon-only 0.1.0 DMG (hierarchical lists, unified index, Dock reopen, visual redesign). Committed directly to `main` (`7e1258a`) per Vincent's confirmation — consistent with how prior releases in this repo were handled (e.g. `743c141`), even though the general house rule is feature-branch-first.

`Scripts/release.sh 0.2.0` ran clean end to end: build → Developer ID sign + Hardened Runtime → DMG → notarized (Accepted) → stapled, 1.9M. Independent re-verification (not just trusting the script's own success message): `stapler validate` OK, and since `spctl -a -t exec`/`-t install` on the **DMG container itself** correctly reports "rejected / no usable signature" (DMGs aren't code-signed objects — only the app inside is, plus the stapled notarization ticket), the real check is mounting the DMG and running `spctl -a -t exec` + `codesign --verify --deep --strict` against the `.app` inside: both passed (`accepted / source=Notarized Developer ID`). Tag `v0.2.0` pushed, GitHub release published with the DMG as a release asset: https://github.com/vincentlauriat/MoveApps/releases/tag/v0.2.0

**Post-release: installed + real-world screenshot (2026-07-07).** Asked to install the DMG and screenshot the app. Installed to `/Applications`, but the main window did **not** appear automatically — this app runs as an agent (`LSUIElement: true`, `showInDock` UserDefaults-driven), and its `Window("main")` scene only opens on first user interaction (menu bar click, or Dock reopen once a Dock icon exists); the `openMainWindow` bridge closure is only captured after the window's first `.onAppear`, so a Dock-reopen event sent before that first appearance is a no-op. macOS's automatic-termination policy actually killed the freshly launched process (~40s later, via a background "Quit AppleEvent") because it never had a visible window — worth remembering for any future automated smoke-test of this app: launching it headlessly and waiting isn't enough, and it won't stay alive on its own without a visible window or menu-bar interaction.

No Accessibility permission was available in this terminal to script a click (same wall as the earlier Dock-click testing attempt — see above), so **Vincent opened the window himself and sent his own screenshot**. Comparing it point-by-point against the pending "visual polish" TODO items: amber/teal root colors, New York serif column titles, small-caps no-icon folder headers, neutral stack-tag tint, tighter card radius — all present and matching the intended design. **Still waiting on Vincent's own explicit "yes this is it" before marking that TODO item done** — don't check it off from Claude's own visual read of a screenshot alone, this is a subjective call that belongs to him ([[feedback_mockup_fidelity]]).

Vincent also decided to **keep `showInDock=true` permanently** (it was `false`/menu-bar-only before this session) rather than reverting the value Claude changed to force a screenshot — a deliberate product decision, not just a leftover of testing.

Unrelated side-quest handled mid-session (not a MoveApps.app change, logged here only for continuity): Vincent asked to dedupe two `vincentlauriat.github.io` checkouts (`~/DevApps/vincentlauriat.github.io` vs `~/DevApps/WebSites/vincentlauriat.github.io`), assuming the `WebSites` one was current. `git log`/`rev-list --left-right --count` showed the opposite — the root copy was in sync with `origin/main` (0/0) while `WebSites` was 4 commits behind. Fast-forward-pulled `WebSites` to match, verified byte-identical with `diff -rq`, then deleted the root copy — outcome matched Vincent's goal (keep `WebSites`) without losing any of the 4 commits.

## Archive checkout lock + per-project size (2026-07-12)

Vincent's framing: turn the Archive into a real multi-Mac referential — a project there is either fully present or just a reference (currently checked out somewhere), never both, with a visible trace of which Mac took it and when so another Mac doesn't take it by mistake. Also wanted disk size on every project line, not just a root-level global total (previously deliberately omitted from `IndexGenerator` for `du`-cost reasons — see the 2026-07-04 index-generation section above).

Confirmed with Vincent before implementing: double check-out is a **hard block** (not warn-and-override), size is shown in **both** the main window and `INDEX.md` via **background/cached** computation (no new `du` on the transfer hot path), and a manual **"Libérer"** action exists for stale/erroneous locks. Full option set and rationale in `PLAN.md` Phase 6.

**Key risk caught before implementing**, via an advisor review of the initial design: the Archive root is iCloud Drive-backed, and a hidden marker file's *content* can be evicted to a `.name.icloud` placeholder locally, reading as empty — which would have silently defeated the whole feature (a Mac would see an "empty" slot and re-take an already-checked-out project). Fix: the checkout marker's **filename** itself (`MOVEAPPS-CHECKOUT__<host>__<date>.json`) carries the essential facts, since names survive eviction (only wrapped with a leading dot + `.icloud` suffix) while content does not; one regex matches both the plain and evicted forms. `CheckoutReferenceStoreTests` constructs a real evicted-placeholder file on disk to prove this holds, not just an assumption.

Implemented by a delegated executor agent on `feature/archive-checkout-lock`, following the `PLAN.md` Phase 6 spec closely. One real, worth-recording deviation found and fixed before commit: the spec's hostname source (`ProcessInfo.processInfo.hostName`) resolves to an ISP reverse-DNS string on Vincent's actual network (`...ipv6.abo.wanadoo.fr`) rather than the Mac's name — caught by the implementing agent testing on the real machine, flagged instead of silently worked around, fixed by switching to `Host.current().localizedName` (`ProcessInfo` kept only as a fallback) before anything was committed.

`CheckoutReferenceStore.read` derives `hostName`/`takenAt` from the marker's (sanitized) filename, never from JSON content alone — `destinationPath`/`sizeBytes` are content-only bonus fields that degrade to `nil` gracefully. `TransferPipeline`'s existing "onyx" safety invariant (critical git-deletion warnings block source deletion) is unmodified and takes priority: the checkout marker is only written when the source is confirmed safely gone (`sourceDeleted && !isCritical`), covered by a dedicated regression test (`criticalPathWritesNoMarker`).

49 tests / 13 suites green (12 new: `CheckoutReferenceStoreTests` ×3, `TransferPipelineTests` ×5, plus `ProjectScannerTests`/`IndexGeneratorTests` extensions), `./Scripts/build.sh` → BUILD SUCCEEDED. Merged to `main` the same day (`eebd5f2`, merge commit) on Vincent's "merge" — rebuilt/retested clean on `main` after the merge. **Not yet validated**: a real two-Mac round trip (take on Mac A, confirm Mac B sees the lock, release, check back in) — only exercised against synthetic fixtures so far, same "not yet exercised in practice" caveat as the original `.icloud` stub-materialization path noted in the legacy rollout section above.

## Release 0.3.0 + Sparkle auto-update (2026-07-12)

Vincent asked to launch the app (worked, but — same known limitation as the 0.2.0 post-release check above — the main window doesn't auto-open for this `LSUIElement` agent app and this terminal has no Accessibility permission to click it; Vincent was asked to click the menu bar/Dock icon himself) and then, separately, to ship a release with auto-update so the new build can reach his other Macs without a manual DMG copy each time.

**The blocker**: `ARCHITECTURE_EN.md` already documented why v1 shipped without auto-update — Sparkle's usual `raw.githubusercontent.com`/GitHub-Releases appcast pattern (the one `MarkdownViewer` uses, confirmed by reading its actual `release-full.sh` before touching anything here) only works over plain unauthenticated HTTP, which requires a **public** repo; MoveApps' was private. Embedding a GitHub token in the distributed app to authenticate a private feed was considered and rejected (secret extractable from the binary, not standard Sparkle behavior). Presented the real options to Vincent (make `MoveApps` public / host the feed on a separate public surface while keeping source private / host on the existing `vincentlauriat.github.io` Pages site) — **he chose to make the repo public**, the same pattern as `MarkdownViewer`.

Implementation mirrors `MarkdownViewer` closely (read its actual `MarkdownViewerApp.swift` and `project.yml` first, rather than guessing at Sparkle's API): `SPUStandardUpdaterController` with `automaticallyChecksForUpdates = true` / `automaticallyDownloadsUpdates = false` (background checks only, install always needs explicit confirmation — chosen deliberately because MoveApps shells out to `git`/`ditto` mid-transfer, where Sparkle silently SIGKILL-ing and swapping the running app would be far more dangerous than in a plain document viewer), a "Rechercher les mises à jour…" command on the `Window("main")` scene, and the standard `SU*` Info.plist keys in `project.yml`. `Scripts/release.sh` gained the Sparkle-signing + `appcast.xml`-writing tail end that `release-full.sh` already had.

New, dedicated EdDSA keychain account `MoveApps` generated via `.sparkle-tools/bin/generate_keys --account MoveApps` (public key `YBYe783LNZGOC/HnoDCO22EU39veSBn0f3u5ARC5c1E=`, now baked into `project.yml`'s `SUPublicEDKey`) — **never to be regenerated**, per the exact warning `MarkdownViewer`'s script documents (a lost/replaced key makes every already-installed copy permanently reject future updates; this happened once to `MarkdownViewer` pre-v0.8.0 and forced a manual re-download for everyone). Private key backed up immediately to `~/.sparkle-keys-backup/MoveApps-private-key-backup.txt` (outside the repo, `chmod 600`) — **flag to Vincent: move this to a password manager and delete the plaintext file**, this was a stopgap, not a permanent secure home for it.

Caught before shipping: `CFBundleVersion` (the build number Sparkle actually compares, not the marketing version) had stayed hardcoded at `"1"` through both `0.1.0` and `0.2.0` in `project.yml` — harmless while no Sparkle feed existed, but would have silently broken update detection (two different versions both claiming build `1` look identical to Sparkle's comparator) had this gone unnoticed. Bumped to `"2"` for `0.3.0`; every future release must bump `CURRENT_PROJECT_VERSION` too, not just `MARKETING_VERSION` — now called out explicitly in `Scripts/release.sh`'s header comment so it isn't missed again.

`./Scripts/release.sh 0.3.0` ran clean end to end and was independently re-verified beyond its own success message: mounted the DMG and ran `spctl -a -t exec` (`accepted / source=Notarized Developer ID`) and `codesign --verify --deep --strict` against the `.app` inside (the DMG container itself isn't a signed object, same caveat noted for 0.2.0 above), plus a read-through of the generated `appcast.xml` confirming the enclosure URL matches the release asset about to be published. Release notes written to `release/release-notes-0.3.0.md` (gitignored, local-only — passed to `gh release create --notes-file`). Published as GitHub release `v0.3.0` with the DMG as an asset, `appcast.xml` committed and pushed to `main` so `raw.githubusercontent.com` serves it.

**Not yet validated**: an actual Sparkle update check/install cycle on a real second Mac (only the appcast/signature mechanics were verified, not an end-to-end "Mac B was on 0.2.0, Sparkle offered and installed 0.3.0" run) — first install on any Mac still has to be the manual DMG per Sparkle's own model (it updates an existing install, it doesn't bootstrap one), only *subsequent* releases benefit from auto-update.

**Local machine cleanup, same session**: Vincent asked to clean up "the versions on the machine" right after the release. Inventory before touching anything: `/Applications/MoveApps.app` was still the stale `0.2.0` install from the 2026-07-07 post-release check; `release/` held all three DMGs (`0.1.0`/`0.2.0`/`0.3.0`); the project's local `build/` (`-derivedDataPath build`) and the global Xcode DerivedData folder for MoveApps together held ~590M of regenerable build output; no stray MoveApps process, no forgotten DMG/DerivedData elsewhere (Downloads, Desktop, other project roots), no mounted volume left attached. Cleaned: `/Applications/MoveApps.app` swapped for the real `0.3.0` DMG's `.app` (re-verified `spctl`/`CFBundleShortVersionString` after the swap, not just assumed from the copy succeeding), the two superseded local DMGs deleted (both still permanently retrievable from their GitHub release tags, so nothing was actually lost), and both build-artifact directories removed (pure cache, regenerate automatically on the next build/release run).

## On-demand Debug window (2026-07-14)

Vincent noted transfers can take a while and asked for a debug window to follow what's happening, open only on demand. Asked him to choose the granularity via an explicit up-front choice: pipeline-step trace (reusing the `TransferStep`s already emitted by `TransferPipeline`) vs. also streaming live subprocess stdout/stderr (git/ditto/npm install), which would require reworking `ProcessRunner` (currently buffers a process's output to completion via `Pipe.readDataToEndOfFile`, no live callback) and every caller (`DirectoryMover`, `NodeModulesInstaller`, `VenvManager`, `GitService`). He chose the pipeline-step level — cheaper, reuses data the pipeline already emits end to end.

New `DebugLogStore` (`MoveAppsUI/Support`, `@Observable`, ring-buffered to 500 entries) is created once in `MoveAppsApp.init` and injected via `.environment` into both the main window and a new `Window(id: "debug")` scene holding `DebugLogView`. `MainWindowViewModel` now takes it as a constructor dependency and logs every `TransferStep` it already consumes from `TransferPipeline.run`'s `AsyncStream` (in both `run(_:)` and `runBatch(_:)`), plus each finished transfer's warnings broken onto their own line, colour-coded by kind (info/warning/success/error derived from `TransferResult.status`). The store always records — logging is just re-rendering text the progress pill already computes, so the cost is negligible — meaning the window shows full history the instant it's opened, even mid-transfer; only the *window* is opt-in (opened via a new toolbar button, never at launch), matching Vincent's "activable à la demande" ask.

Also extracted `TransferHistoryView`'s private French `warningText(_:)` into `ProjectListing.describe(_ warning:)` so the history sheet and the new debug log render warnings identically instead of duplicating the mapping.

Build (`xcodebuild -scheme MoveApps`) and `MoveAppsCoreTests` (49/49) reverified green after regenerating the Xcode project (`xcodegen generate`) to pick up the two new files. **Not yet manually verified**: the actual click-through — opening the window from the toolbar, watching it fill live during a real transfer, auto-scroll, the "Effacer" button — none of that is exercised by the build/unit-test check, since it's pure SwiftUI GUI behavior; flagged to Vincent as needing a real-use confirmation.
