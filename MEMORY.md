---
name: move-apps-project
last_updated: 2026-07-03 (Phase 5, in progress)
---

# Project Memory — MoveApps

## Purpose
`move-app.sh` migrates Vincent's ~126 development projects (~38 GB) from `~/Documents/Github` (iCloud Drive-synced) to `~/DevApps` (local, not iCloud-synced), one or a few at a time, keeping each project immediately usable (git intact, Python venvs working, no dangling absolute paths). Its one-shot bulk rollout is **complete** (see "FINAL ROLLOUT SUMMARY" below) — `move-app.sh` is now kept as an unmaintained CLI fallback/reference.

Since 2026-07-02, the active focus of this project is **MoveApps.app**, a native SwiftUI macOS app that replaces the CLI for ongoing, bidirectional GitHub⇄DevApps transfers across Vincent's Macs. See "MoveApps.app (SwiftUI rewrite) — project state" below and `PLAN.md` for the current phase.

## Current state
- **Full bulk rollout COMPLETE (2026-07-01 → 2026-07-02).** All 126 projects have been migrated from `~/Documents/Github` to `~/DevApps`. Tool built, tested read-only first (`--list`, `--dry-run` on `AuditViewer` and `clarify`), validated with one real migration (`LinkManager`), then rolled out in 7 batches of ~15, each migrated and independently re-verified (git branch/HEAD/dirty-count re-checked with `git status --porcelain`, venv sanity via real `pip list`, broken-symlink scan excluding build artifacts, grep for residual absolute paths, confirmation the source folder is gone).
- `~/Documents/Github` now contains only 3 non-project leftovers (`CLAUDE.md`, `CommitTousLesDossiers.sh`, `MetsAJourLesGIT.sh`) — 0 project folders remain.
- Batch 1/7 done (`Chronos`, `clarify`, `claude-hooks-demo`, `claude-mem`, `Claude.md.PlusPlus`, `ClaudeCodePontUX`, `ClaudeCommander`, `ClaudeControlPlane`, `ClaudeHooks`, `ClaudeSettings`, `CLI-Airtable`, `CLI-Plaud`, `CLI-Starter`, `CLIPrint`, `ContextManager`): 14/15 clean, 1 warning on `clarify` (see below). `claude-mem` (937M) and `ClaudeCommander` (698M) hit the known `mv`-timeout and fell back to `ditto` successfully.
- Batch 2/7 done (`curl.md`, `CV`, `datagouv-mcp`, `DiagnosticsReseau`, `Electron`, `feynman`, `Ficelles`, `Finance`, `FinceptTerminal`, `Gatsby`, `glances`, `GlobalTimeSheet`, `gmailreader`, `graphify`, `headroom`): 13/15 clean, 2 warnings (`Gatsby`, `glances`, see below). `FinceptTerminal` (6.0G, largest project moved so far), `feynman` (787M), `Finance`, `Gatsby` (1.1G), `glances` (784M), `headroom`, `curl.md` all hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 3/7 done (`hermes`, `homebrew`, `iCloudMonitor`, `immocarto`, `Inspirations`, `InternetCheck`, `InvestFinder`, `IPP-app`, `JobTracker`, `KnowledgeBuilder`, `KnowledgeLib`, `KnowledgeMarker`, `linkedin`, `llmfit`, `localhostmgr`): 15/15 clean, no warnings. `KnowledgeBuilder` (2.0G) has 10814 dirty git files but that count matches pre-migration exactly (already dirty before, not caused by the move). `immocarto`, `Inspirations`, `InvestFinder`, `IPP-app`, `KnowledgeMarker` hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 4/7 done (`LocalWebManager`, `LocationManager`, `LocaWeb`, `MarkdownViewer`, `Medium`, `MenuBarManager`, `minutes`, `MLXTest`, `my-video`, `MyTasks`, `NatureSite`, `nDocMake`, `NetCheck`, `NetDiscoPocket`, `NetDiscoWeb`): 13/15 clean, 2 warnings (`MyTasks`, `NetCheck`, see below). `MLXTest` (2.1G) venv recreated successfully with real packages (`mlx`, `mlx-lm`, etc). `LocalWebManager` (2.5G), `LocaWeb`, `Medium`, `MLXTest`, `my-video`, `MyTasks`, `NetCheck` hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 5/7 done (`NewsManager`, `Nexus`, `notion-cookbook`, `NotionBook`, `NotionCLI`, `NotionToObsidian`, `oh-my-claudecode`, `onyx`, `opendataloader-pdf`, `OpenTelemetry`, `Opentelemetry 2`, `OsxPhotos`, `Plaud`, `plaud-sdk-public`, `printing-press-library`): 14/15 clean, 1 warning on `onyx` — **real data loss, recovered**, see critical finding below. `oh-my-claudecode` (3302 dirty files, matches pre-migration exactly, not caused by the move). `NewsManager` (1.1G), `Nexus` (590M), `NotionBook`, `onyx` (1.0G), `printing-press-library` (883M) hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 6/7 done (`prompts.chat`, `ProjectTemplate`, `ProxyStory`, `RaybanMeta`, `RemoteClaude`, `rtk`, `RTKInfos`, `SkillManager`, `Skills`, `SkillsMarketInvestFinder`, `SkillsMgr`, `smartcolibri.fr`, `StockList`, `symphony`, `TeamsChat`): 14/15 clean, 1 warning on `symphony` (benign, see below). `StockList`'s (1.0G) venv recreated with real packages. `ProxyStory`, `RTKInfos` (714M), `SkillManager` (778M), `StockList` (1.0G), `symphony` (119M) hit the `mv`-timeout and fell back to `ditto` successfully.
- Batch 7/7 done, the FINAL batch (`TechDoc`, `TestNotionDoc`, `TestSites`, `toolkit`, `TVNews`, `UltraData`, `viewer-fixtures`, `VLTKYC`, `vMemory`, `Voltalia`, `VoltaliaTasks`, `WebAppUsage`, `WebManager`, `WhatsUp`, `WhatsUp.vibe.vincent`, `WifiManager`, `worldmonitor`, `zeroclaw` — 18 projects): 17/18 clean, 1 warning on `WifiManager` (same `.sparkle-tools` cross-project symlink issue as `NetCheck` in batch 4) — fixed the same way, re-pointed to `~/DevApps/MarkdownViewer/.sparkle-tools`. `TestSites`, `VoltaliaTasks` (675M), `WifiManager` (452M), `worldmonitor` (288M) hit the `mv`-timeout and fell back to `ditto` successfully.
- **121 total projects migrated** (13 pre-existing/manual + 108 via the 7 batches = 15×6 + 18). Rollout complete.
- Orphan empty venv `~/DevApps/LinkManager/path/to/venv` has been removed.
- Inventory at last scan: 76 git repos (remotes verified real for several, all pointing to GitHub), 30 Node projects, 30 Xcode projects, 8 Rust, 5 Python (pyproject.toml), 4 Python (requirements.txt), 1 Go. 8 real Python venvs found (`clarify`, `glances`, `LinkManager` — plus an orphan `path/to/venv` inside it —, `Gatsby/GatsbyExecution`, `Gatsby/GatbyViewer`, `Gatsby/MomentumBot`, `StockList/backend`, `MLXTest`).
- No `.icloud` (un-materialized) stub files found at last scan — all content was locally cached, so that code path is implemented but not yet exercised against a real stub.

## FINAL ROLLOUT SUMMARY — all 7 batches, complete picture
121 projects migrated total, 0 left in `~/Documents/Github`. Across all 108 batch-migrated projects: 100 fully clean, 8 flagged with a warning by the script. Of those 8:
- 3 were **empty Python venvs** (script's `pip freeze` 30s timeout too short): `clarify/.venv`, `Gatsby/GatbyViewer/venv`, `glances/.venv`. **All 3 fixed post-rollout** (2026-07-02): `glances` and `GatbyViewer` reinstalled from their `requirements.txt`; `clarify`'s venv only serves the `bmad-story-automator` skill tool (no runtime deps) so `story-automator` itself was installed editable — all verified working.
- 2 were **benign concurrent-edit false alarms** (dirty-count changed mid-move but only `M`/`??` entries, nothing lost): `MyTasks`, `symphony`.
- 2 were **broken cross-project symlinks**, both `.sparkle-tools` pointing to `MarkdownViewer`'s old path: `NetCheck`, `WifiManager` — both fixed by re-pointing to the new `~/DevApps/MarkdownViewer/.sparkle-tools`.
- **1 was real data loss, recovered: `onyx`** — `ditto` silently dropped 3 tracked files (183 lines of real code) during its fallback copy on a 1.0G repo. Recovered fully via `git restore` after confirming `.git fsck` was clean. This is the single most important finding of the whole rollout — see CRITICAL finding section below for full detail. It proves `ditto`'s fallback (used whenever plain `mv` hits the iCloud timeout, which was most large repos) is not 100% reliable on large trees, and the only reason it was caught was that all migrated projects started with a *known* pre-migration dirty count that the script diffs against.
- Several other projects had a lot of pre-existing dirty files (`KnowledgeBuilder` 10814, `oh-my-claudecode` 3302, `DiagnosticsReseau` 420, `WhatsUp.vibe.vincent` 70, `WhatsUp` 52, `rtk` 58) — all independently confirmed identical before/after, not caused by the migration.
- Python venvs that recreated successfully with real packages: `LinkManager/.venv`, `MLXTest/.venv` (mlx/mlx-lm), `Gatsby/MomentumBot/.venv` + `bot/venv` (trading bot, ~60 packages), `Gatsby/GatsbyExecution/venv`, `StockList/backend/.venv` (fastapi/anthropic/etc).

## Known issue found during testing
Plain `mv` between `~/Documents/Github/*` and `~/DevApps/*` can fail with `Operation timed out` even though both paths report the same filesystem device ID (`stat -f "%d"`) — iCloud Drive's FileProvider extension intercepts the `rename()` syscall. The script now falls back to `ditto` + verification automatically; this is not a hypothetical edge case, it reproduced on the very first real test (`LinkManager`), so expect it to trigger often on the remaining 125 projects.

## Known warning — clarify
`clarify`'s `.venv` was recreated empty (no `pip freeze` captured before the move). No `requirements.txt`/`pyproject.toml` at the project root either — it's a Node/Turbo monorepo, and the venv likely only serves the `bmad-story-automator` skill tool (`pyproject.toml` under `.claude/skills/bmad-story-automator/` and `.agents/skills/bmad-story-automator/`). Not blocking; flag to Vincent if he needs that skill's Python env — reinstall from that subpackage's `pyproject.toml`.

## Known warning — Gatsby and glances (batch 2)
`Gatsby` has 4 nested Python venvs; 3 recreated fine with real packages (`MomentumBot/.venv`, `MomentumBot/bot/venv` — a real trading-bot venv with ~60 packages incl. `ib-insync`/`anthropic`/`pandas`, and `GatsbyExecution/venv`), but `GatbyViewer/venv` came back empty. `glances`'s `.venv` also came back empty. Independently verified with `pip list` on each venv — confirms the script's own report. Root cause: the script's `pip freeze` capture has a 30s timeout (`with_timeout 30`), which can be too short for large/slow venvs before the move. Both `GatbyViewer` and `glances` do have `requirements.txt` (`glances` also has `pyproject.toml`) at their root, so a plain `pip install -r requirements.txt` after the move fixes it — just not automatic. Worth watching for on other large Python projects in later batches (e.g. `StockList/backend`, `MLXTest` still pending).

## Known warning — MyTasks and NetCheck (batch 4)
`MyTasks` finished with a NEW kind of warning, different from the empty-venv pattern: "nombre de fichiers modifiés différent avant/après (0 -> 6)" — the git dirty-count itself changed during the move window. Investigated with `git status --porcelain` / `git diff`: this is real, legitimate in-progress work (a design doc edit describing a `web/` subfolder restructuring, `.gitignore` change, new untracked `package.json`/`package-lock.json`/`.mcp.json`/`commandes.md`). Conclusion: something (another session/tool) was actively writing to `~/Documents/Github/MyTasks`'s working tree during the migration window, between the script's before/after git snapshots — not a migration bug, nothing lost or corrupted, just uncommitted local work that changed mid-move. Worth telling Vincent in case he had an editor/agent open on `MyTasks` he forgot about.

`NetCheck` finished with a warning for 4 broken symlinks. 3 are Xcode build/DerivedData artifacts under `build/` (self-referential, harmless — they regenerate on next build). The 4th was real: `.sparkle-tools` symlinked to `MarkdownViewer`'s `.sparkle-tools` folder at the old `Documents/GitHub/MarkdownViewer` path (cross-project symlink between two projects both migrated in this batch). Fixed directly: re-pointed `~/DevApps/NetCheck/.sparkle-tools` to `~/DevApps/MarkdownViewer/.sparkle-tools`, verified the link resolves and lists real Sparkle framework files. General lesson: cross-project symlinks (one project linking into another's folder) are a blind spot the script's own residual-path grep doesn't catch since it only scans file contents, not symlink targets — worth an eye out for more of these in later batches.

## CRITICAL finding — onyx (batch 5): ditto can silently drop tracked files
`onyx` finished with the same warning shape as `MyTasks` ("nombre de fichiers modifiés différent avant/après (0 -> 3)"), but the cause was different and more serious: `git status --porcelain` showed 3 files as **deleted** (`D`), not modified/added — `backend/onyx/connectors/dropbox/__init__.py`, `backend/onyx/connectors/dropbox/connector.py` (183 lines of real code), and `extensions/chrome/src/.DS_Store`. Confirmed on disk: the whole `dropbox/` connector folder was genuinely missing after the move, and the source at `~/Documents/Github/onyx` was already gone (no fallback copy). This means `ditto` silently failed to copy some files on this 1.0G repo during the `mv`-timeout fallback — a real bug/limitation, not a hypothetical.

Recovery: verified `.git` integrity first (`git fsck` clean, `git cat-file -p HEAD:<path>` could read the file from history), then ran `git restore <paths>` — fully recovered, `git status` now clean, `connector.py` back to its original 183 lines. Recovery worked because the files were tracked in git (uncommitted deletions can always be restored from HEAD as long as `.git` itself is intact); untracked files lost the same way would NOT be recoverable this way.

**Standing procedure going forward**: the script's dirty-count-mismatch warning conflates two very different scenarios that look identical in its own message — (a) benign concurrent edits (see `MyTasks` below) and (b) real file loss from an incomplete `ditto` copy (this case). Whenever a batch reports this specific warning type, always run `git status --porcelain` on the affected project and look specifically for `D ` (deleted) entries — if found, verify `.git fsck` is clean and immediately `git restore` them. The numeric-count check itself is reliable as a detector (any tracked-file loss changes the count and gets flagged — verified via `git_snapshot()`'s implementation, which is a plain `git status --porcelain | wc -l`, so a real loss can't hide behind a coincidentally-matching count for projects that started at 0 dirty files, which was true for most projects in batches 1-5). Not yet checked: whether any *untracked* file could have been silently dropped the same way without triggering a count change — that would be undetectable by this method. No user files are known to be at risk since none of batches 1-5's `AVERTISSEMENT` entries besides `onyx` showed a `D ` pattern.

## Known warning — symphony (batch 6)
`symphony` finished with the same dirty-count-mismatch warning shape (0→2), but the standing procedure cleared it fast: `git status --porcelain` showed one `M` (modified `elixir/WORKFLOW.md`) and one `??` (`.DS_Store`) — no `D` entries, so no data loss, just a benign concurrent edit like `MyTasks`. Also found 2 broken symlinks in `symphony/elixir/_build/{test,dev}/phoenix-colocated/symphony_elixir/node_modules` — both are *relative* symlinks (`../../../../assets/node_modules`) pointing to a `node_modules` folder that was never installed in `elixir/assets/` in the first place (pre-existing, unrelated to the migration, self-heals on `npm install`). `StockList`'s `backend/.venv` recreated successfully with ~real packages (fastapi, anthropic, etc — confirmed with `pip list`).

## How to apply
When helping Vincent with further migrations or issues in this tool, assume: bash 3.2 only (no `mapfile`, no associative arrays, no GNU `timeout`/`fzf` guaranteed installed), same-volume-but-still-slow iCloud moves, and that safety (never delete unverified) matters more than speed here.

## MoveApps.app (SwiftUI rewrite) — project state

Started 2026-07-02, right after the bash rollout finished. Vincent wants the same GitHub⇄DevApps circulation as an ongoing, bidirectional, GUI tool usable on this Mac and his other Macs — not a one-shot CLI script anymore.

**Key decisions** (see `~/.claude/plans/zesty-discovering-alpaca.md` for full detail):
- Full native Swift reimplementation of the bash logic, not a wrapper around `move-app.sh`. `move-app.sh` stays as reference/fallback, unmaintained.
- macOS 26.0 / Swift 6.0 / strict concurrency, XcodeGen, split `MoveAppsCore`/`MoveAppsUI`/`MoveApps` — matches the newest convention already used by `~/DevApps/NetworkTools/NetCheck` (explored read-only to confirm bundle ID pattern `com.vincent.<App>`, Sparkle-via-SPM setup, `SMAppService` login item pattern, `Process`+`AsyncStream` idiom for shelling out to `git`/`ditto`).
- Bidirectional (GitHub→DevApps and DevApps→GitHub), unlike the bash script which only ever went one way.
- **Repo is private** — this breaks the `raw.githubusercontent.com` Sparkle appcast pattern NetCheck/WifiManager use (needs a public repo). Decision: v1 ships with no network auto-update, DMG distributed manually between Macs; a private Sparkle feed is a later nice-to-have, not a blocker.
- The `onyx` ditto-data-loss bug (see CRITICAL finding above) is the single most important behavior to preserve in the port: the pipeline must diff git dirty-count before/after and specifically flag `D ` (deleted) entries as a first-class `.critical` result, not a generic warning — that's literally how the bash script caught it, and the Swift port must not regress on this.
- Test strategy for the `onyx` case without touching real projects: a `DirectoryCopying` protocol behind `DittoCopier`, with a `FaultInjectingCopier` test double that reproduces "file count matches but a tracked file was silently dropped" on a real local git fixture repo (`git init`/`commit` via `Process`, not mocked) under `FileManager.default.temporaryDirectory`.

**Progress**: Phase 0 (scaffolding) done 2026-07-02 — `project.yml`, target skeletons, `Scripts/{fetch-sparkle-tools,build,release}.sh`, `git init`, Debug build + Swift Testing smoke test both green.

**Phase 1 (Core logic + tests) done 2026-07-02** — full `MoveAppsCore` implementation (25 Swift files) + 9 test files, 17 tests / 8 suites, independently re-verified green (build + test rerun outside the implementing agent). The `onyx` reproduction test passes: a fault-injected copier drops a tracked file while keeping the file count equal, and the pipeline correctly escalates to `.critical` with the source preserved.

Two implementation-level findings worth remembering for future Swift work in this repo:
- `Process.waitUntilExit()` can hang intermittently under Swift 6 strict concurrency (races with concurrent pipe reads via its private `CFRunLoop`) — use `terminationHandler` + `DispatchGroup` instead, as `ProcessRunner` now does.
- `FileManager` is not `Sendable`; expose it as a computed `.default` property in `actor`/`Sendable` types rather than storing an injected instance, to avoid `@unchecked Sendable`.
- Deliberate deviation from `move-app.sh`: `TransferPipeline` defers source deletion on the copy/ditto fallback path until *after* the post-move git snapshot is compared — the bash script deleted first. This is what makes the `onyx`-style bug provably non-destructive in the Swift port (native rename is atomic and still deletes immediately, since there's no partial-copy risk to guard against).

**Phase 2 (menu bar UI) done 2026-07-02** — `MoveAppsApp.swift` (3 Scenes), `RootPathsController` (NSOpenPanel + bookmark persistence), menu bar quick-pick list wired to `TransferPipeline`, Settings (root pickers, login item). Build green, `MoveAppsCoreTests` untouched and still 17/17. **Not independently visually verified** — this environment has no screen recording/Accessibility access, so the menu bar appearance, Settings panel, `NSOpenPanel` flow and login-item toggle need Vincent to click through interactively before Phase 3 builds on top of them. Known deliberate limitation: true security-scoped bookmarks need sandbox entitlements the app can't have (it shells out to `git`/`ditto`); `RootPathsController` falls back to plain bookmarks with identical UX.

**Repo GitHub (2026-07-02)**: private repo `vincentlauriat/MoveApps` created and pushed (`origin`, branch `main`, 3 commits). Note for future sessions: creating a GitHub repo is not actually blocked by any technical guardrail (gh CLI is authenticated with `repo` scope) — it was withheld pending explicit user confirmation as a "visible/hard-to-reverse action on a shared platform" caution, per the global "Executing actions with care" instructions. Ask before creating repos, but don't claim a hard technical block if asked why.

**Phase 3 (main window + history) done 2026-07-02**, on branch `feature/phase3-main-window` (not yet merged/pushed — Vincent asked to defer doc/git housekeeping and "avancer sur l'application", so this is uncommitted on purpose, pending his review). Implemented by an `executor` agent (opus) from a detailed prompt covering the full existing API surface, independently re-verified afterward (re-ran `xcodebuild -scheme MoveApps -configuration Debug build` → BUILD SUCCEEDED with zero concurrency warnings, and `xcodebuild test -scheme MoveAppsCoreTests` → 17/17 tests green, both re-run outside the implementing agent, not just trusting its self-report).

New files under `Sources/MoveAppsUI/MainWindow/` (replacing `MainWindowPlaceholderView.swift`, which was deleted):
- `MainWindowViewModel.swift` — `@MainActor @Observable`. Reuses `QuickPickViewModel.scanSync`/`.describe` (static, same module) rather than duplicating the project-scan/step-description logic; deliberately does NOT share mutable state with `QuickPickViewModel` (menu bar and main window can each run a transfer independently). Owns a `TransferHistoryStore` pointed at `~/Library/Application Support/MoveApps/history.json` (first real wiring of history persistence — previously only exercised by tests with an injected temp URL). Exposes `pendingPlan` (drives the confirmation sheet) and the same `isRunning`/`currentStepText`/`lastResult` pattern as `QuickPickViewModel`.
- `MainWindowView.swift` — two-column layout (Archive/`~/Documents/GitHub` left, Active/`~/DevApps` right). Drag & drop via a `DraggedProject: Codable, Transferable` wrapper (`CodableRepresentation(contentType: .json)`) with `.draggable`/`.dropDestination(for:)` — macOS 26 native API, no third-party drag helper. A per-row arrow button remains as a non-drag alternative (accessibility/discoverability). Inline progress banner while `isRunning`, no separate progress sheet.
- `TransferPlanView.swift` — confirmation sheet: project, `from → to`, `keepSymlink`/`reinstallNode` toggles, Confirm/Cancel.
- `TransferHistoryView.swift` — past `TransferRecord`s, most recent first, colored status, French-translated `TransferWarning` detail per case.

`MoveAppsApp.swift` now builds `MainWindowViewModel` in `@State` and injects it into `Window("MoveApps", id: "main")`, which renders `MainWindowView()`. `MenuBarExtra`/`Settings` scenes untouched.

**Phase 3 committed 2026-07-03** on branch `feature/phase3-main-window` (commit `4cd5041`, "feat: build main window UI (Phase 3)") — still not merged to `main`, Vincent chose to keep building rather than pause for git housekeeping.

**Phase 4 (packaging) started 2026-07-03**, same branch. `Scripts/release.sh` (a full codesign/notarize/DMG pipeline) already existed from the Phase 0 scaffolding and turned out to be essentially complete — this phase was mostly about closing small gaps and *proving the pipeline actually works*, not writing it from scratch:
- Added `LSApplicationCategoryType` (`public.app-category.developer-tools`) and `NSHumanReadableCopyright` to `project.yml`'s `info.properties` for the `MoveApps` target. Important gotcha found: XcodeGen treats `project.yml`'s `info.properties` as the source of truth and regenerates `Sources/MoveApps/Info.plist` from it on every `xcodegen generate` — editing the `.plist` file directly gets silently clobbered on the next generate. Always edit `project.yml`, not the plist.
- `Sources/MoveApps/MoveApps.entitlements` is an empty `<dict/>` and was judged **correct as-is**, not a gap: the app is deliberately non-sandboxed (it shells out to `git`/`ditto` via `Process`, which the sandbox forbids) and requests no special hardened-runtime capability (no JIT, no unsigned memory, no library-validation exception needed since all embedded frameworks — including `Sparkle.framework` — are signed with the same Developer ID identity as the app). Empty entitlements + Hardened Runtime enabled at the codesign step is a standard, valid combination for this shape of app.
- `Scripts/release.sh` gained a `SKIP_NOTARIZE=1` env-var mode: runs the full build→codesign→DMG pipeline and stops before `xcrun notarytool submit`, so the pipeline can be validated without the notarization credentials being set up yet.
- **Ran the dry-run pipeline for real** (`SKIP_NOTARIZE=1 ./Scripts/release.sh 0.1.0`): Release build succeeded, every nested binary (Sparkle's `Autoupdate`/XPC services/`Updater.app`, both frameworks, the app itself) signed with the real `Developer ID Application: Vincent LAURIAT (KFLACS69T9)` identity — already present in the login keychain from prior work (same identity pattern as `NetCheck`/`WifiManager`), **no keychain UI prompt was needed**, so this pipeline can run non-interactively. Hardened Runtime confirmed on (`flags=0x10000(runtime)` in `codesign -dv` output). DMG produced (1.3M) and independently re-verified (not just trusting the script's own success message): mounted it, ran `codesign --verify --deep --strict` (valid on disk) and `spctl -a -t exec` (accepted, source=Developer ID) directly against the mounted `.app`.
- **AppIcon deferred by design, not a blocker**: `Assets.xcassets/AppIcon.appiconset` has all 10 required slots declared in `Contents.json` but zero actual image files — a placeholder. This does not fail the build (`actool` just produces an icon-less app), so v1 can ship without custom artwork; treated as cosmetic polish, not in scope unless Vincent wants to prioritize it.
- Sparkle.framework is embedded and gets signed but has **zero code wiring** (`grep` for `Sparkle`/`SPUStandardUpdater` in `Sources/` returns nothing, no `SUPublicEDKey`/`SUFeedURL` anywhere) — consistent with the existing decision (no auto-update feed for v1, private repo). It's dead weight in the bundle for now, harmless.

**Remaining real blocker for Phase 4 — needs Vincent, not automatable**: notarization. `xcrun notarytool history --keychain-profile "MoveApps-Notary"` confirms no credentials are stored. Vincent must run, once, in his own interactive terminal (needs his real Apple ID + a freshly generated app-specific password from appleid.apple.com — not something that can be scripted or done on his behalf):
```
xcrun notarytool store-credentials "MoveApps-Notary" --apple-id "vincent@lauriat.fr" --team-id "KFLACS69T9"
```
After that one-time setup, `./Scripts/release.sh 0.1.0` (no env var) produces a fully signed, notarized, stapled, distributable DMG. Suggest he run it himself via `! ./Scripts/release.sh 0.1.0` so he can see/approve any keychain prompt interactively, even though the dry run above suggests none should appear.

**Phase 4 committed 2026-07-03** on `feature/phase3-main-window` (commit `9cebda8`).

**Phase 5 (cross-validation) started 2026-07-03**, same branch, no code changes (validation-only, uses disposable scratch test files that are never committed — written, run via `xcodebuild test`, then deleted each time). Vincent chose "both" when asked how to handle the round trip: synthetic first for safety, then a real project he nominates.

- **Stack-detection cross-check**: an isolated fixture project (git+node+python markers) in the scratchpad — never under the real `~/Documents/GitHub`/`~/DevApps` — run through `move-app.sh --dry-run` (with `SRC_ROOT` env-var override, a supported override per the script's own design) and through `StackDetector` via a throwaway Swift Testing test. Both produce exactly `git node python`. Confirms the Swift port's stack detection algorithm matches the bash original bit-for-bit on a representative fixture, on top of the existing `StackDetectorTests.swift` unit coverage.
- **Synthetic round trip on the REAL configured roots** (`RootLocations.default` = real `~/DevApps`/`~/Documents/GitHub`, not temp dirs): a disposable git repo created directly in `~/DevApps`, moved through `TransferPipeline` `.active → .archive` then `.archive → .active` for real. This is the first time the `active → archive` direction has ever executed against the actual production root paths — all 121 real prior migrations went the other way. Both legs returned `.ok`, git log and file content were byte-identical before/after, and both `TransferRecord`s correctly round-tripped through the real `TransferHistoryStore` at `~/Library/Application Support/MoveApps/history.json` (proving the Phase 3 UI's history persistence path end-to-end, not just in temp-dir unit tests) — those 2 synthetic entries were then deliberately removed from the real history file afterward so Vincent's actual transfer history stays authentic. The synthetic test project self-deletes via a `defer` block covering both possible resting locations, regardless of pass/fail.
- **Remaining for Phase 5**: a round trip on a real, already-migrated, low-stakes project that Vincent nominates (asked, not yet answered as of this checkpoint) — the synthetic test proves the mechanics but not project-specific quirks (real venvs, real git history depth, real symlinks) the way a genuine project would.

Next: once Vincent names a project, repeat the round trip against it for real (same throwaway-test pattern, same rigorous before/after verification). Then: notarization once Vincent sets up credentials (see Phase 4), and Vincent's interactive click-through of the menu bar UI (still pending from Phase 2).
